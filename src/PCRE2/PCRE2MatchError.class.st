"
I represent a matching or substitution failure reported by PCRE2.

The binding raises me for negative return codes that are real errors rather than ordinary no-match results.
"
Class {
	#name : 'PCRE2MatchError',
	#superclass : 'PCRE2Error',
	#category : 'PCRE2',
	#package : 'PCRE2'
}
